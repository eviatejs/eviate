import { config } from '../src/interfaces/config';

const config: config = {
  port: 3000,
  hostname: 'localhost',
  debug: true,
  startMiddlewares: [],
  endMiddlewares: []
};

export default config;
